# 🏆 [브론즈 2] OX 퀴즈 (Baekjoon 8958)
# 문제 설명: "OOXXOXXOOO"와 같은 OX 퀴즈의 결과가 있습니다. 문제를 맞으면(O) 점수를 얻는데, 
# 연속으로 맞으면 점수가 1점씩 커집니다. 틀리면(X) 연속 점수는 0점으로 리셋됩니다.


def solution(quiz_result):
    total_score = 0  # 전체 총점
    current_score = 0 # 현재 연속 정답 점수 (O가 나올 때마다 커짐)

    for answer in quiz_result:
        if answer == "O":
            current_score +=1
            total_score +=current_score
        elif answer == "X":
            current_score = 0
    # 힌트: 'O'면 current_score를 1 늘리고 total_score에 더한다.
    #       'X'면 current_score를 0으로 만든다.
    
    
    return total_score

# 테스트
print(solution("OOXXOXXOOO"))       # 예상: 10
print(solution("OXOXOXOXOXOXOX"))   # 예상: 7
print(solution("OOOOO"))            # 예상: 15 (1+2+3+4+5)
print(solution("XXXXX"))            # 예상: 0

# 🏆 [브론즈 3] 최댓값 (Baekjoon 2562)
# 문제 설명: 서로 다른 9개의 자연수가 주어집니다. 이 중에서 최댓값을 찾고, 그 최댓값이 몇 번째 수인지를 구하세요.
# 예시: 만약 숫자가 리스트로 [3, 29, 38, 12, 57, 74, 40, 85, 61] 이렇게 들어온다면?
# 가장 큰 수는 85입니다.
# 85는 8번째에 있습니다. (컴퓨터 인덱스로는 7번이지만, 문제에서는 '몇 번째'냐고 물었으니 1부터 세어야 합니다.)
# 출력:85
#      8

def solution(numbers):
    max_num = 0       # 가장 큰 수를 담을 변수 (일단 0으로 시작)
    max_index = 0     # 가장 큰 수의 위치를 담을 변수
    
    # [힌트]
    # 1. for문을 돌면서 하나씩 꺼냅니다.
    # 2. '지금 꺼낸 숫자'가 '현재 max_num'보다 크다면? -> 왕위를 계승합니다.
    # 3. 그때의 '위치(index)'도 기록해둡니다. (enumerate를 쓰면 편합니다!)

    for i, num in enumerate(numbers):
        if num > max_num:
            max_num = num
            max_index = i+1
    return max_num, max_index

# 테스트
data = [3, 29, 38, 12, 57, 74, 40, 85, 61]
result_val, result_idx = solution(data)

print(f"최댓값: {result_val}")  # 예상: 85
print(f"위치: {result_idx}")    # 예상: 8