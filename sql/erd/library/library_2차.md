#### 도서관
- 책을 읽어요. -> 책이 있어야해요.
    도서관은 책을 관리한다.
    => 도서관이 필요하다.
- 책을 빌려요.
    - 반납 - 대출 + return date로 표현
    - 연체 - due_date < 오늘날짜 인 경우여서
            SELECT를 할때 WHERE로 처리 가능
```
연체된 책 조회
SELECT * 
FROM BOOK
JOIN BOOKITEM
JOIN RENT
WHERE RENT.dutdate < 오늘날짜
```
            따로 컬럼을 두게 된다면 스케쥴러를 통해
            매일 확인해야 함 -> 굳이...
    - 연체에 대한 페널티를 부가해볼까? - 비즈니스 로직
        - 1. 대출 금지 기간 설정
            사람에 금지 기간을 부여
                대출 시도 -> 기간보다 이르면 금지
                            기간 이후면 null로 변경
        - 2. 벌금 부여
            새로운 테이블을 만들고
            대출과 1:1 관계를 맺는다.
            -> 자주 생기는 일이 아니여서 column에 넣기는 굳이...
            추가로 user와 역정규화도 고려할 수 있습니다
                벌금이 있는데 안냈어? 대출불가 같은 기능때문에
    - 예약
    - 분실
![alt text](image-1.png)